cd /var/www/Marketing

# Reset or stash any local uncommitted changes on EC2 server (e.g. package-lock.json or temporary config edits)
git stash
git checkout main
git pull origin main

# Rebuild Frontend SPA
npm install
npm run build

# Update Backend & Restart API Process
cd backend
npm install
npx prisma db push
pm2 restart aimarketing-api

# Reload Web Server
sudo systemctl reload nginx

